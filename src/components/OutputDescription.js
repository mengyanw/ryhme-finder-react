const OutputDescription = (props) => {
    const {outputDescription} = props
  return (
    <div className="row">
      <h2 className="col">{outputDescription}</h2>
    </div>
  );
};

export default OutputDescription;
